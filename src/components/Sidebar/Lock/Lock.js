import React from "react";
import * as classes from "./Lock.module.css";

const Lock = () => {
  return (
    <div className={classes.lock}>
      <button>Lock</button>
    </div>
  );
};

export default Lock;
